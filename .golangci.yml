version: "2"

linters:
  default: standard
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
    - revive
  exclusions:
    paths:
      - gen/
      - note/
      - docs/

formatters:
  enable:
    - gofmt
    - goimports
  exclusions:
    paths:
      - gen/
      - note/
      - docs/

run:
  timeout: 5m
