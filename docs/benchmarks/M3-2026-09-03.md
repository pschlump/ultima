# M3 Pub/Sub Benchmark Report — 2026-09-03

Milestone M3 exit criterion: redis-benchmark pub/sub (design doc §14.4).

## Environment

- Machine: Apple M4 Max, 14 cores, 36 GB RAM (darwin/arm64)
- Ultima: `ultima-server 272c4ef-dirty (commit 272c4ef8a924970c79bbde6baff06e1f97c970a1, branch master, built 2026-09-04T03:22:48Z for darwin/arm64)` (shards: 4×GOMAXPROCS, power of two)
- Redis: `Redis server v=7.2.7 sha=00000000:0 malloc=libc bits=64 build=7f24f11dd7e42c58` (via REDIS_BIN=redis-server; `--save '' --appendonly no`)
- Driver: `redis-benchmark 7.2.7`, PUBLISH benchchan payload, -n 300000 per run (-n 100000 for -c 1)
- Subscribers: 8 × `note/pubsub-bench-sub` (Go discard-and-count reader;
  redis-cli formats every message and cannot drain fast enough, which trips
  Ultima's slow-consumer close — a client artifact, so it was not used)
- Ultima config: `{"server":{"resp_addr":"127.0.0.1:7379","log_level":"error"}}`

## Headline numbers (requests/sec, higher is better)

| Workload | Ultima | Redis 7.2.7 | Ultima/Redis | Delivery check |
|---|---|---|---|---|
| PUBLISH, 0 subscribers,  -c 50 -P 16 | 1851851.88 | 1840490.75 | 1.01x | no delivery expected |
| PUBLISH, 8 subscribers,  -c 50 -P 16 | 64850.84 | 773195.88 | 0.08x | Ultima 4/8 subs up (4/8 full, min=5250); Redis 8/8 subs up (8/8 full, min=300000) |
| PUBLISH, 8 subscribers,  -c 50 -P  1 | 28384.90 | 137048.89 | 0.21x | Ultima 8/8 subs up (8/8 full, min=300000); Redis 8/8 subs up (8/8 full, min=300000) |
| PUBLISH, 8 subscribers,  -c  1 -P  1 | 14923.15 | 9103.32 | 1.64x | Ultima 8/8 subs up (8/8 full, min=100000); Redis 8/8 subs up (8/8 full, min=100000) |

Numbers are single-run means from redis-benchmark's summary output;
rerun `make bench` for a fresh sample. Both servers ran on the same
loopback interface, sequentially, with no other benchmark load.

Delivery check: subscriber connections still attached after the run
(PUBSUB NUMSUB) and per-subscriber received-message counts from the
driver. Ultima closes a subscribed connection whose push queue exceeds
4096 messages (slow consumer, the analogue of Redis's
client-output-buffer-limit pubsub 32mb/8mb/60, which is far more
generous), so at full publisher speed some or all Ultima subscribers
are dropped by design; subscribers that stay attached receive every
message. The `-c 50 -P 16` fan-out row is therefore unstable
run-to-run for Ultima — the mean blends throttled fan-out with fast
zero-subscriber publishing after the drops. The `-P 1` rows, where
all subscribers on both servers received all messages, are the stable
apples-to-apples comparison.
