
The web front end should have an interactive window that just allows you to
run Ultima commands, and returns with the results in a second area at the
bottom of the screen. It is essentially the same as the CLI but as a
web front end.  This should also work for things that result in
content being pushed from the server like SUBSCRIBE / PSUBSCRIBE.


